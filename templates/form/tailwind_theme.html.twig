{% use 'tailwind_2_layout.html.twig' %}

{% block form_row %}
    {% set row_class = row_class|default('form-row') %}
    {{ parent() }}
{% endblock form_row %}

{% block widget_attributes  %}
    {% set widget_class = widget_class|default('form-widget') %}
    {{ parent() }}
{% endblock widget_attributes  %}

{% block form_label %}
    {% set label_class = label_class|default('form-label') %}
    {{ parent() }}
{% endblock form_label %}

{%- block checkbox_widget -%}
    {%- set widget_class = widget_class|default('form-checkbox') -%}
    {{- parent() -}}
{%- endblock checkbox_widget -%}
