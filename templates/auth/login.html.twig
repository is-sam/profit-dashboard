{% extends "base-empty.html.twig" %}

{% block title %}Sign In{% endblock %}

{% block body %}
    <div class="mt-40 max-w-lg mx-auto px-10">
        <h3 class="text-3xl mb-10">{{ 'auth.login.title' | trans }}</h3>

        {{ form(loginForm) }}
    </div>

{% endblock %}
